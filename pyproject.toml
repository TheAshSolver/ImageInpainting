[project]
name = "imageinpainting"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aryan Kulkarni", email = "aryan.kulkarni@research.iiit.ac.in" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "pillow>=12.3.0",
]

[project.scripts]
imageinpainting = "imageinpainting:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
